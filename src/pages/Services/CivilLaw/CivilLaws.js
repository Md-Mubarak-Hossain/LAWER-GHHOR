import React from 'react';
import useTitle from '../../../hooks/useTitle';

const CivilLaws = () => {
    useTitle('Civil law');
    return (
        <div>
            <h2>civil laws</h2>
        </div>
    );
};

export default CivilLaws;